# Dependencies
node_modules/
vendor/

# Build output
dist/
build/
bin/
*.o
*.exe
food-brain
mcp-server

# Env & secrets
.env
.env.*
!.env.example
*.pem
*.key

# Go
*.test
*.out
coverage.*

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# Local data
*.sqlite
*.db
data/
tmp/

# OpenSpec cache
.specsync/

# Skein runtime state (machine-local, not project source)
# Exception: the testing policy and test command ARE project source — commit them
# so the verify/debugger gate can read the correct (Go) test command in every worktree.
.skein/*
!.skein/steering/
!.skein/test-command.txt
logs/

# per-user runtime config (copy of family.example.json)
family.json
